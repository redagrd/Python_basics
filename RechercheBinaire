"""
Recherche binaire:

Cet algorithme de recherche est un peu plus compliqué 
mais plus performant que la recherche linéaire
car au pire des cas on ne cherchera que dans la moitié du tableau

Il n'est utilisable qu'avec des tableaux triés.

Le principe est de comparer l'élément à trouver avec l'élément 
au milieu du tableau:

    si le milieu du tableau est inférieur à l'element a trouver
     on divise le tableau en 2 et on répéte la recherche 
     sur cette moitié du tableau
    si le milieu du tableau est supérieur à l'element a trouver 
    on divise le tableau en 2 et on répéte la recherche sur cette moitié 
    du tableau

C'est un cas typique d'utilisation de la récursivité

"""
import random

arr = []
elem = 42    # element a trouver dans le tableau
cptr = 0

# remplissage du tableau avec 500 valeurs aléatoires comprisent entre 0 et 1000
for i in range(20):
    arr.append(random.randrange(0, 1000))

arr.sort()
"""
arr = [30, 446, 823, 652, 861, 138, 697, 860, 863,42 , 885, 411, 875, 408, 787, 510, 120, 533, 354, 294]
# fonction de tri de python
arr.sort()

# Fonction de recherche binaire, elle prend en paramètre l'élément a trouver,

# le tableau dans lequel chercher, et les indice du début et de la fin du tableau.
def binarySearch(elem, arr):
        find = False
        position = len(arr)//2 # ou int(), on prend l'element au milieu du tableau
        if arr[position] == elem:
            find = True
            print("l'element est trouvé")
        elif arr[position] > elem:
            del arr[position:]
        elif arr[position] < elem:
            del arr[:position]
        binarySearch(elem, arr)
        # affichage
        if len(arr) == 1:
             print("aucun élément n'a été trouvé")
        elif find == True:
             print("l'élément est trouvé")
        else: 
            print("l'élément n'est pas trouvé")
             
binarySearch(elem, arr)
"""
def binarySearch(elem, arr, debut, fin):
    debut == 0
    find = False
    milieu = debut + (fin - debut) //2
    if arr[milieu] == elem:
            return milieu
    elif arr[milieu] > elem:
            del arr[milieu:fin]
            return binarySearch(elem, arr, debut, milieu-1)
    else:
            return binarySearch(elem,arr, milieu+1, fin)
    binarySearch(elem,arr,debut,fin)
    else:
    return "not find"
binarySearch(elem,arr,debut,fin)

position = binarySearch(elem, arr, 0, len(arr) -1)
if position != "not find":
    print("l'élement", elem, "a été trouvé en position:", position)
else:
    print("l'élement", elem, "n'a pas été trouvé")

position = binarySearch(elem, arr, 0, len(arr) +1)